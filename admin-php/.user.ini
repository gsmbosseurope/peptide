; Raises PHP's upload limits for this folder on Hostinger (which reads
; .user.ini on shared hosting). Without this, large product photos/videos
; may fail to upload even though MAX_UPLOAD_BYTES in config.php allows them.
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 120
